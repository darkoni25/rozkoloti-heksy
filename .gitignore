# ── Godot 4 ───────────────────────────────────────────────────────────────
# Кеш імпорту та внутрішній стан редактора (відновлюється автоматично)
.godot/

# Android-збірки
/android/

# Експортовані бінарники та паки
*.exe
*.pck
*.apk
*.aab
*.zip
*.tar.gz

# Export presets (може містити ключі підпису)
export_presets.cfg

# ── Claude Code ───────────────────────────────────────────────────────────
# Зберігаємо .claude/ (там settings.json з налаштуваннями проекту)
# але ігноруємо локальні overrides
.claude/

# ── Дизайн-артефакти (не ігровий код) ────────────────────────────────────
design_handoff_main_menu/

# ── Тимчасові файли ───────────────────────────────────────────────────────
*.tmp
*.log

# ── ОС-специфічні ─────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
